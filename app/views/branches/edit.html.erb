<% @page_title = t(:branch).pluralize(locale) %>

<%= link_to("<< " + t(:return_to), [@current_user, params[:controller]], :class => 'back-link') %>

<div class="branch edit">
  <h2><%= t(:update) + " " + t(:branch) %></h2>

  <%= form_for([@current_user, @branch]) do |f| %>

    <%= render(:partial => "form", :locals => {:f => f}) %>

    <div class="form-buttons">
      <%= submit_tag(t(:edit) + " " + t(:branch)) %>
    </div>

  <% end %>

</div>