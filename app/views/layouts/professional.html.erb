<!DOCTYPE html>
<html>
  <head>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Timesheet | <%= @page_title || "Profesional" %></title>
    <%= stylesheet_link_tag('application', :media => 'all') %>
    <%= javascript_include_tag('public') %>
  </head>
  <body>


    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to("Timesheet Profesional", professionals_path, :class => "navbar-brand") %>
          
        </div>
        <div id="navbar" class="navbar-collapse collapse">

          <ul class="nav navbar-nav">
            <li><%= link_to(t(:company).pluralize(locale), professional_companies_path(:professional_id => "1")) %></li>
            <li><%= link_to(t(:client).pluralize(locale), professional_clients_path(:professional_id => "1")) %></li>
            <li><%= link_to(t(:appointment).pluralize(locale), professional_appointments_path(:professional_id => "1")) %></li>
          </ul>  
          <ul class="nav navbar-nav navbar-right">
            <li>
              <% if I18n.locale == I18n.default_locale %>
                
                <%= link_to(image_tag('gb.png', :size => '22x14', :alt => 'gb') + " English", :locale=>'en') %>
              <% else %>
                <%= link_to(image_tag('co.png', :size => '22x14', :alt => 'co') + " Español", :locale=>'es') %>
              <%end%>
            </li>
          </ul>          
        </div><!--/.navbar-collapse -->
      </div>
    </nav>

    <div class="container">

      <div class="jumbotron">
          <!--/.Application RB Begins-->

          <h1 class="page-header"><%#= t(:professional).pluralize(locale) %></h1>

          <div id="main">
            <% if !flash[:notice].blank? %>
              <div class="notice">
                <%= flash[:notice] %>
              </div>
            <% end %>
            <div id="content">
              <%= yield %>
            </div>
          </div>

          <!--/.Application RB Ends-->
        
        </div>
     
   
    </div>

    

    <div id="footer">
      <p id="copyright"> &copy; mg.com / MG</p>
    </div>
  </body>
</html>