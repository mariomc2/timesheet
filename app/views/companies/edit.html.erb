<% @page_title = t(:company).pluralize(locale) %>

<%= link_to("<< " + t(:return_to), [@current_user, params[:controller]], :class => 'back-link') %>

<div class="companies edit">
  <h2><%= t(:update) + " " + t(:company) %></h2>

  <%= form_for([@current_user, @company]) do |f| %>

    <%= render(:partial => "form", :locals => {:f => f}) %>

    <div class="form-buttons">
      <%= submit_tag(t(:edit) + " " + t(:company)) %>
    </div>

  <% end %>

</div>